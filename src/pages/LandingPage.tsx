import { AdminPage } from "@/components/layout/AdminPage";

export default function LandingPage() {
  return (
    <AdminPage
      title="Association Administration"
      description="Configure cohorts, maintain previous levels, manage the drill library, and set up session drill weighting for your evaluation season."
    />
  );
}
